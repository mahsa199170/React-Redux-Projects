import React from 'react';

const Count = () => {
  return (
    <div>
      count
      <button>Add</button>
      <button>Subtract</button>
      <button>Reset</button>
    </div>
  );
};

export default Count;
