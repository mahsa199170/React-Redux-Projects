import React from 'react';

const Count = () => {
  return (
    <div>
      count
      <div>
        <button style={{ padding: 10 }}>Add</button>
        <button>Subtract</button>
        <button>Reset</button>
      </div>
    </div>
  );
};

export default Count;
