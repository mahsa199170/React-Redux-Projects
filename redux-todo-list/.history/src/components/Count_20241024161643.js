import React from 'react';

const Count = () => {
  return (
    <div>
      count
      <div>
        <button>Add</button>
        <button>Subtract</button>
        <button>Reset</button>
      </div>
    </div>
  );
};

export default Count;
